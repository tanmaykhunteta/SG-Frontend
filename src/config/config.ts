import { environment } from "src/environments/environment";

export let config = {
        ACC_TOKEN_NAME :  "authToken", //local storage name
        USER_DATA_NAME : "user", //session storage name
        ACC_TKN_HDR : 'x-access-token',
        MIN_REGISTRATION_AGE : 14
    }
